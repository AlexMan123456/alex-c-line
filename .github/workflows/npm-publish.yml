name: publish npm

permissions:
  id-token: write

on:
  push:
    branches:
      - main

jobs:
  publish:
    if: ${{ github.repository == 'AlexMan123456/alex-c-line' }}
    uses: AlexMan123456/github-actions/.github/workflows/npm-registry-publish.yml@030badcc6072a9ed75003761be464e6641f63cf4 # v2.13.0
