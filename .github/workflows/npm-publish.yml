name: publish npm

permissions:
  id-token: write

on:
  push:
    branches:
      - main

jobs:
  publish:
    if: ${{ github.repository == 'AlexMan123456/alex-c-line' }}
    uses: AlexMan123456/github-actions/.github/workflows/npm-registry-publish.yml@67ff3e75e5c0930b4598f4a188225234dda25ba9 # v2.14.0
