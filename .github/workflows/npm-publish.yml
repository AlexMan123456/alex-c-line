name: publish npm

permissions:
  id-token: write

on:
  push:
    branches:
      - main

jobs:
  publish:
    if: ${{ github.repository == 'AlexMan123456/alex-c-line' }}
    uses: AlexMan123456/github-actions/.github/workflows/npm-registry-publish.yml@134246f0ec5c5b7efad0b3e332a68c387132b84f # v3.1.2
